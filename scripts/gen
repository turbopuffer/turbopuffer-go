#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

api_gen_image=ghcr.io/turbopuffer/turbopuffer-apigen:31b9eeb16562e501c9f7f89010c96da49e887693

echo "==> Generating code"
docker run -v "$(pwd):/work" "$api_gen_image" go > custom_types.go
